<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="dialog_with_black_alpha_background" parent="@android:style/Theme.Dialog">
        <item name="@android:windowFrame">@null</item>
        <item name="@android:windowIsFloating">true</item>
        <item name="@android:windowIsTranslucent">true</item>
        <item name="@android:windowNoTitle">true</item>
        <item name="@android:windowBackground">@android:color/transparent</item>
        <item name="@android:windowCloseOnTouchOutside">false</item>
    </style>

    <style name="Dialog_Anim_Style_Bottom_In" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/bottom_enter_anim</item>
        <item name="@android:windowExitAnimation">@anim/bottom_out_anim</item>
    </style>

</resources>
